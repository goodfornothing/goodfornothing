<section id="primary_content">
	<h1>Friends, making gigs possible</h1>
	
	<div class="abstract">
		<p>Good for Nothing does cost a few quid to put on. Luckily there are some awesome companies and organisations out there who have supported us through donating products, services, space and time to help us put on a great experience. We thank you very much, you are all awesome.</p>
	</div>
	
	<% if @friends.any? %>
		<ul class="grid_list">
		<% @friends.each do |friend| %>
		<li>
			<% if friend.logo.present? %>
				<%= link_to image_tag(friend.logo, :alt => friend.name), friend.url %>
			<% else %>
				<%= link_to(friend.name, friend.url, :class => "logo") %>
			<% end %>
		</li>
		<% end %>
		</ul>
	<% end %>
	
</section>

<aside id="secondary_content">
	<p class="cta"><%= link_to "Fuel a gig", new_partner_path %></p>
</aside>