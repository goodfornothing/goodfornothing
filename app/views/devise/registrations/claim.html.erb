<section id="primary_content" class="single">
	
	<%= simple_form_for(@user, :as => 'user', :url => activate_path(@user.id,@user.ning_profile.id), :html => { :method => :put }) do |f| %>

		<h1>Claim your profile</h1>		
		
		<%= "<p id='site_notice'>#{flash[:error]}</p>".html_safe if flash[:error] %>
		<%= "<p id='site_notice'>#{flash[:notice]}</p>".html_safe if flash[:notice] %>
		<%= "<p id='site_notice'>#{flash[:alert]}</p>".html_safe if flash[:alert] %>
		
		<p>Welcome to our new web hive <%= @user.short_name %>, you're nearly there, just enter new password below to claim your profile.</p>

		<fieldset>
			<%= f.input :password, :required => true,  :input_html => { :autocomplete => "off" }, :wrapper_html => { :class => "smart_label" } %>
			<%= f.input :password_confirmation, :required => true, :input_html => { :autocomplete => "off" }, :wrapper_html => { :class => "smart_label" } %>
		</fieldset>

		<fieldset class="actions">
			<%= f.hidden_field "reset_password_token", :value => params[:secret] %>
			<%= f.button :submit, "Let me at it" %>
		</fieldset>
		
		<p class="footnote">Oh, yeah, by re-activating your account you are agreeing to our new, fairly standard and dull, <%= link_to "privacy", privacy_path %> policy.</p>
		
	<% end %>

</section>