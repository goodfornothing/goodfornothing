<% if @categories %>
<h2>Conversations</h2>
	<ul>
		<li><%= link_to "All posts", blog_posts_path %></li>
		<% @categories.each do |category| %>
		<li><%= link_to category.title, categorised_blog_posts_path(category.slug) %></li>
		<% end %>
	</ul>
<% end %>